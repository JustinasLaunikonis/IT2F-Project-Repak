Input Fields:
Input keys are case-sensitive and must match the JSON keys and Word placeholders exactly.
The fields below occur in the current Word template unless explicitly marked JSON-only.

format:
placeholder (English translation + description): data type/format

Details on all pages:
[meldingnummer] (report number): int
[distributeur] (distributor): string
[monteur] (technician): string
[klant] (client): string
[tijdverschil] (time difference): signed offset string, for example "+1" in the JSON
[datum] : dd-mm-yyyy
[engineer] (engineer): string
[checked by] (the person who checked the report) : string


1.1:
[machinenummer] (machine number of the machine in question): string
[contactpersoon] (contact person): name string
[telefoonnummer en/of email] (contact details): email and/or phone number string
[contactpersoon aanwezig bij de klant] (contact person on-site at the customer's location): string
[probleem] (problem description): long string
[urgent] (is the issue urgent?): string, for example "yes" in the JSON
[Service Informatieblad beschikbaar voor eerstelijnsadvies] (service information sheet available for first-line advice): string, for example "yes" in the JSON
[wens of storing] (request or malfunction): string, for example "malfunction" in the JSON
[bouwjaar] (construction year/date): string; the current JSON uses dd-mm-yyyy

2:
[communicatietype] (communication type): string, for example "phone" in the JSON
[oorspronkelijke melding] (original report): long string containing the original report or call transcript

3:
[installatie] (installation date): dd-mm-yyyy
The current template has no FAT placeholder, and the JSON has no FAT key.

3.1.1:
[samenvatting melding] (summary of report): long string

3.1.2.:
[instructie] (instructions): long string

The current template has no text placeholders in sections 3.1.3 and 3.1.4.

3.2:
The current template has no advice placeholder, and the JSON has no advice key.

JSON-only field:
[mail aan] (mail recipient): email address string
This key exists in the JSON but has no matching placeholder in the current Word template,
so the current replacement code does not insert its value into the document.
